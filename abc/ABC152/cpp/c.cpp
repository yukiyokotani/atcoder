#include <iostream>;
#include <vector>using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
const int MOD = 1E9 + 9;
const int INF = 1<<29; 

int main(){
    int N;
    vector<string> P(N);
    cin >> N;
    rep(i, n) cin >> P(i);
    
}